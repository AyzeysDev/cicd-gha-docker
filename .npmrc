# This setting tells pnpm to create a flat node_modules structure,
# similar to npm or yarn. This is more compatible with Docker and
# other CI/CD environments as it avoids issues with symbolic links.
node-linker=hoisted